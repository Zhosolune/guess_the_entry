# DeepSeek API Key Configuration
# 
# 推荐使用 DEEPSEEK_API_KEY。
# 该变量仅在本地开发服务器（Vite Proxy）或服务端环境（Vercel）中生效。
# 它不会被打包进前端代码，因此是安全的。
DEEPSEEK_API_KEY=sk-your-api-key-here

# 注意：请勿使用 VITE_ 前缀的变量名存储敏感 Key。
# VITE_ 前缀的变量在构建时会被注入到前端代码中，存在泄露风险。
